# Security policy

## 凭据处理

- 工具包只从当前进程或当前 Windows 用户的 `OPENROUTER_API_KEY` 环境变量读取 Key。
- Key 不会写入 TOML、模型目录、设置文件、日志、备份清单或命令行参数。
- 设置脚本使用隐藏输入，并在转换后清理临时明文引用。
- 推荐使用专用、低额度 Key，并定期轮换。

用户级环境变量由 Windows 保存，适合个人设备上的便捷使用。共享设备或高安全需求环境应使用短期进程变量，并在退出 PowerShell 后确认其失效。

## 远端目录

模型目录下载限定为 `https://openrouter.ai`，拒绝重定向，设置超时和 25 MB 上限，并检查：

- JSON 可解析性；
- 模型数量上下限；
- slug 字符集与唯一性；
- 配置的默认模型；
- 可支持的提示字段；
- 所有远端提示已被本地轻量提示覆盖。

未通过检查的新目录不会替换现有有效目录。

## 本地修改

- 安装器只管理带项目标记的 Profile 区块。
- 正式文件通过随机临时文件复读校验后原子替换。
- 安装、更新和卸载会创建本地备份。
- 卸载只移动工具包拥有的目录；Key 只有在显式指定时才会删除。

本地备份可能包含原 Codex 配置中的敏感内容。请让备份留在当前用户的 `.codex` 目录，不要提交到 Git。

## 报告安全问题

请在 GitHub 仓库中提交不含敏感数据的 Issue。请先删除 API Key、用户名、邮箱、私人路径、配置正文、日志和请求内容。高敏感漏洞请避免公开披露，改用 GitHub 提供的私密漏洞报告功能（仓库启用后）。
