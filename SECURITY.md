# Security policy

## 凭据处理

- 目录刷新可从当前进程或当前 Windows 用户的 `OPENROUTER_API_KEY` 环境变量读取 Key；`cxor` 启动 Codex Desktop 时要求有效的用户级 Key。
- Key 校验接受 OpenRouter 的 `sk-or-` 前缀，并拒绝空白或控制字符。
- Key 不会写入 TOML、模型目录、设置文件、日志、备份清单或命令行参数。
- 模型 ID 与推理强度会拒绝 API-Key 形态的输入，并在任何安装写入前停止；错误信息不会回显该输入。
- 设置脚本使用隐藏输入，并在转换后清理临时明文引用。
- 推荐使用专用、低额度 Key，并定期轮换。

用户级环境变量由 Windows 保存，适合个人设备上的便捷使用。设置或删除后，脚本会向 Windows Shell 广播环境变化；已经运行的进程仍可能保留旧值。短期 `Process` 变量适合目录刷新与临时诊断，无法作为 Windows 打包桌面应用的可靠推理凭据。禁止持久保存 Key 的高安全环境应停止使用该桌面端 OpenRouter 路由，或仅在隔离 PowerShell 中执行目录维护。轮换后运行 `cxor` 重启桌面端；删除后关闭既有桌面进程。

## 认证职责

- 持久化 OpenRouter provider 通过 `env_key = "OPENROUTER_API_KEY"` 读取推理请求所需的 Key。
- 模型目录刷新使用临时 `CODEX_HOME` 内的隔离 `command` auth；它只服务于目录刷新进程，并在清理临时目录时移除。
- 升级会移除旧版持久化 `auth` 子表；inline 或 dotted `auth` 声明会触发安全拒绝，避免桌面端长期执行认证命令。
- CI 不接收真实 OpenRouter Key，也不执行 live smoke、联网目录刷新或推理请求。

## 远端目录

OpenRouter 的公开 `GET /api/v1/models` 当前返回顶层 `data` 数组。工具包检测到该结构后立即进入受信 Codex CLI 的隔离兼容刷新，生成顶层 `models` 目录。公开响应不会直接用作 `model_catalog_json`。

直接目录请求固定为 `https://openrouter.ai`，使用 Bearer 认证，拒绝重定向，并实施总超时、JSON `Content-Type`、25 MB 上限和严格 UTF-8 检查。兼容刷新对子进程输出施加 25 MB 上限，目录落盘前还会检查：

- JSON 可解析性；
- 顶层 `models` 结构、关键字段类型与模型数量上下限；
- slug 字符集与唯一性；
- 配置的默认模型；
- 可支持的提示字段；
- OpenRouter Key 模式等敏感属性名与属性值，包括 JSON Unicode 转义解码后的字符串；
- 所有远端提示已被本地轻量提示覆盖。

未通过检查的新目录不会替换现有有效目录。

## 本地修改

- 安装器只管理 PowerShell Parser 识别出的真实注释标记区块；here-string 中的同形文本会保留。
- `Write-Utf8FileAtomic` 管理的文本先写入目标目录中的随机临时文件，再替换单个目标文件；该保证只覆盖调用此函数的写入路径。
- Windows 下的新建备份、暂存、事务目录、活动安装目录和 `previous-install` 使用受保护的私有 DACL；Owner 与唯一 FullControl 规则都指向当前进程用户，目录规则向子目录和文件传播。每个目录树项目都会拒绝重解析点并复读权限。目录复制只采信经过清单验证的相对路径、长度和摘要，来源目录 ACL 不会传播到目标树。
- 受管文件快照按“ACL 前读、共享锁内字节与元数据、ACL 后读”生成；内容、时间、长度或 Owner/Group/DACL 任一项变化都会终止操作。安装清单另存原文件的 Owner、Group 与 DACL 策略；正式恢复时重建原有保护状态与显式规则，并让未保护文件按原目标父目录重新继承。
- 所有整文件快照都会在已打开的锁定流内再次执行大小检查。配置、Profile、settings 与清单采用 5 MiB 上限，模块文件采用 25 MiB 上限，目录与缓存采用 50 MiB 上限，未知类型的共同 helper 兜底上限为 100 MiB。文本在解析前执行严格 UTF-8 解码；超限、截断、增长或无效编码会在发布前终止。
- 模块与旧安装目录树限制为 1,024 个项目、单文件 25 MiB、文件总量 64 MiB；共同目录 helper 默认限制为 4,096 个项目。遍历采用流式枚举并在达到边界时停止，避免宽目录树先被完整物化。恢复事务清理允许更高的聚合总量，但仍受单文件、项目数量与 512 MiB 总量限制；超限事务目录会保留并告警。
- 恢复敏感字节前，现有目标会先切换到当前用户私有 ACL；内容、时间和摘要复读完成后才应用清单中的目标 ACL。新目标从空的私有暂存文件开始，并在完整写入和复读前保持私有。失败时会从内存初始快照重建原内容；可疑或无法清理的 rollback 会保留并报告路径。
- 安装、恢复、卸载，以及运行时供应商配置与模型缓存切换的回滚采用内容、时间与 ACL 的事务产物 CAS。外部改写、并发新建或目录树 inventory 变化会被保留并报告冲突；工具包只删除仍与本事务产物一致的普通文件或私有目录树。目录刷新是独立的前置维护动作，成功提交后不会因后续供应商切换失败而撤销。
- 常规非提权模式不会加载、保存、比较或恢复 SACL。带审计要求的文件不在 ACL 保真保证范围内，原子替换可能影响其审计元数据。清单中显式含 `S:` 的 ACL 输入会被拒绝；工具包无法据此识别磁盘上的全部 SACL。
- 临时、rollback、discard、delete、refresh 或 ACL 失败副本无法删除时会告警或抛错，并报告残留路径与内容风险。工具包不会跟随可疑链接清空这些路径；请核对路径身份后手动删除。仓库忽略规则覆盖这些随机文件名，降低误提交风险。
- 模型提示重写会在临时文件中重新读取并校验模型数量与提示内容，然后替换目录文件。目录移动另有私有 ACL、inventory 与 CAS 校验。
- 安装、更新和卸载会创建本地备份。schema 3 安装清单记录五个受管文件的摘要、`LastWriteTimeUtc`、Windows Owner/Group/DACL SDDL 元数据，以及旧安装目录的逐文件长度和 SHA-256；恢复器继续兼容 schema 1/2。原子复制、正式恢复与失败回滚会复读文件时间，避免旧模型目录因恢复操作获得新时间戳。
- 恢复器在跨进程锁内重新检查当前安装状态，只移动通过工具包身份校验的安装目录。事务回滚未完整完成时会保留快照，并在错误中报告路径。
- 默认卸载会恢复保存的 OpenAI 配置与可用的 OpenAI 模型缓存，并移动工具包拥有的目录；Key 只有在显式指定时才会删除。

本地备份可能包含原 Codex 配置中的敏感内容。请让备份留在当前用户的 `.codex` 目录，不要提交到 Git。

整文件原子写入为了支持 CAS、回滚和提交后复读，短时间内会同时持有少量内容副本。类别上限用于约束内存峰值；处理接近上限的目录或缓存时仍需预留额外内存。对更大文件应先缩减或移出受管路径。

备份摘要用于发现意外损坏，以及清单生成后的单独文件变化。清单与备份内容位于同一用户可写目录，未使用外部真实性锚；能够以同一用户身份同时改写两者的进程也能重算摘要。管理员、SYSTEM、已控制当前用户进程的代码，以及能够持有父目录句柄并参与极窄重命名竞态的同用户进程，均位于本地信任边界内。恢复前应确认备份目录来源可信，并依赖 Windows 账户、磁盘与目录 ACL 限制写入权限。

## 报告安全问题

请在 GitHub 仓库中提交不含敏感数据的 Issue。请先删除 API Key、用户名、邮箱、私人路径、配置正文、日志和请求内容。高敏感漏洞请避免公开披露，改用 GitHub 提供的私密漏洞报告功能（仓库启用后）。
