# Changelog

## 0.1.1 - 2026-08-27

- `cxor` 每次运行都会检查目录有效性与年龄，并在达到阈值后刷新。
- 针对公开 `/api/v1/models` 的顶层 `data` 格式，改用经过认证的 Codex CLI 兼容目录刷新流程。
- 区分推理使用的 provider `env_key` 与仅服务目录刷新的隔离 `command` auth。
- API Key 校验接受 `sk-or-` 前缀，并提示用户级 Key 轮换后重新打开 PowerShell。
- 默认卸载恢复可用的 OpenAI 模型缓存。
- 修复 TOML 注入、PATH 阴影 CLI、恶意恢复清单、设置路径篡改与宽泛桌面进程结束问题。
- 增加 schema 2 受管文件哈希与旧安装逐文件摘要、固定目标恢复、工具包 ownership 校验、跨进程锁、切换/安装/卸载事务回滚及原 ACL 保留。
- 修复恢复等待期间的并发安装误删、非工具包目录误删、旧安装备份篡改及回滚失败后快照丢失。
- 修复 quoted/Unicode TOML 键、表头注释括号、inline/dotted/父级 array-table 冲突和四引号多行字符串边界。
- 将旧版持久化 command auth 迁移到 `env_key`，并拒绝不明确的 inline/dotted auth 配置。
- 增加目录敏感值递归检查与流式子进程输出上限，覆盖 Unicode 转义和大输出边界。
- Profile 受管区块改用 Parser 注释令牌定位，避免删除 here-string 正文。
- 规范化锁路径尾分隔符，并接管 abandoned mutex，确保安装、恢复和卸载共享同一锁语义。
- 单文件原子回滚失败时保留旧内容快照并报告恢复路径。
- 修复目标文件缺失时恢复流程丢失备份 ACL，并让文件暂存与事务快照保留来源 ACL。
- 修复旧安装目录复制中断后遗留半成品目录，并在目录树恢复中保留和复读校验 Windows ACL。
- 拒绝把 API-Key 形态的值写入模型或推理参数，且校验错误不回显可疑输入。
- 修复合法 TOML 多行嵌套数组被误判为表头的问题。
- 原子临时文件或失败副本无法删除时会报告残留路径与内容风险，并增加对应忽略规则。
- 修复空文件安装、重复安装覆盖原设置、提示重写改变目录年龄、标量 `models` 与异常模型字段类型问题。
- 增加对抗性安全回归，并收窄单文件临时替换和提示写入后读回校验的公开保证。
- CI 增加超时及 PowerShell 7.4/当前稳定版矩阵；live smoke 保持本机手动执行，CI 不接收真实 Key。

## 0.1.0 - 2026-08-27

- 提供 `cx` / `cxor` Codex Desktop 供应商切换。
- 动态发现 PowerShell Profile、Codex Home 和 Codex CLI。
- 自动刷新和验证 OpenRouter 模型目录。
- 支持新旧两种 Agent 提示字段。
- 提供 1,362 字符轻量 Agent 提示。
- 提供隐藏输入的 API Key 设置与轮换。
- 提供安装、备份、卸载、恢复、状态和测试流程。
- 加入远端目录大小、重定向、slug 和重复项检查。
